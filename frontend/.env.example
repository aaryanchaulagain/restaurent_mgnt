# =============================================================================
# Frontend environment template (placeholders only)
# NEVER put server secrets in NEXT_PUBLIC_* variables.
# =============================================================================

# --- Public (browser-exposed) ---
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
NEXT_PUBLIC_PARTNER_TERMS_VERSION=2026-07-01
NEXT_PUBLIC_REQUIRE_SUPER_ADMIN_MFA=true
# Local demo catalogue fallback — disable in production
NEXT_PUBLIC_ENABLE_DEMO_FALLBACK=false

# --- Server-only (if added later; never prefix with NEXT_PUBLIC_) ---
# API_INTERNAL_URL=
# STRIPE_SECRET_KEY=   # DO NOT put Stripe secret keys here — backend only
